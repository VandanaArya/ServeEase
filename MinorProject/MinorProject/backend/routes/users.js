const express = require('express');
const router = express.Router();
const User = require('../models/User');

// Required fixing for auth.js in server.js, just making sure the logic flows
// Also need to add bookings to server.js
